import java.io.File;import java.util.HashMap;public class macbook {    public static void main(String[] args) {            File file = new File("/Users/zhangchu/Pictures/xin");            File[] list = file.listFiles();            HashMap<String,String> map = new HashMap<String,String>();            map.put("李明1", "123123");            map.put("李明2", "123123");            map.put("李明3", "123123");            // 如果目录下文件存在            if (file.exists() && file.isDirectory()) {                for (int i = 0; i < list.length; i++) {                    //取文件名子存入name中                    String name = list[i].getName();                    // 截取.之前的字符串出来                    int index = name.indexOf(".");                    // 截取.JPG出来                    int index2 = name.lastIndexOf(".");                    String name1 = name.substring(0,index);                    String name3 = name.substring(index2);                    // 拼接字符串                    String newName =  name1+ ("_") +map.get(name1);                    //重命名                    File dest = new File("/Users/zhangchu/Pictures" + "/" + newName+name3);                    list[i].renameTo(dest);                    System.out.println(dest.getName());                }            }    }}